package xyz.terrific.utils.version

/**
 * @author TerrificTable55
 */
class Version
    /**
     * Version... thingy
     * @param name name of client / version
     * @param installed if client / version is installed
     */
    (val name: String, var isInstalled: Boolean)

